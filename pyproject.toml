[project]
name = "takeovers-bilan"
version = "0.1.0"
description = "Grounded extraction of French annual-account fields"
requires-python = ">=3.11"
dependencies = ["PyMuPDF>=1.28,<2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
