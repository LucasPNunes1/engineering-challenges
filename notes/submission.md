# Submission

This branch is the delivery snapshot for the Bilan challenge. It is opened as a
pull request against `main` so the implementation, validation notes, and commit
history can be reviewed in one place.

The implementation itself is documented in the root `README.md` and
`notes/engineering-decisions.md`.
